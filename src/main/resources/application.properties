# =================================
# Configuration de base Spring Boot
# =================================
spring.application.name=api
server.servlet.context-path=${CONTEXT_PATH:/api}
server.port=${SERVER_PORT:8080}

# =================================
# Configuration Base de donnees
# =================================
spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:soskate}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

# =================================
# Configuration JPA/Hibernate
# =================================
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.generate_statistics=false

# =================================
# Configuration gestion d'erreurs
# =================================
spring.mvc.throw-exception-if-no-handler-found=true
spring.web.resources.add-mappings=false

# =================================
# Configuration des logs
# =================================
logging.level.root=INFO
logging.level.com.soskate.api=DEBUG
logging.level.org.springframework=WARN
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
spring.output.ansi.enabled=ALWAYS

# =================================
# Configuration Jackson (JSON)
# =================================
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.date-format=yyyy-MM-dd'T'HH:mm:ss
spring.jackson.deserialization.fail-on-unknown-properties=false
spring.jackson.default-property-inclusion=NON_NULL

# =================================
# Configuration validation
# =================================
spring.mvc.problemdetails.enabled=true

# =================================
# Configuration performance
# =================================
spring.datasource.hikari.maximum-pool-size=20
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.max-lifetime=1800000

# =================================
# Configuration developpement
# =================================
spring.devtools.restart.enabled=true
spring.thymeleaf.cache=false

# =================================
# Configuration monitoring
# =================================
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=when-authorized
management.info.env.enabled=true

# =================================
# Profils Spring
# =================================
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}

# =================================
# Configuration CORS
# =================================
soskate.cors.allowed-origins=http://localhost:4200,http://localhost:8081,http://localhost:19006
soskate.cors.allowed-methods=GET,POST,PUT,DELETE,PATCH,OPTIONS
soskate.cors.allowed-headers=*
soskate.cors.allow-credentials=true
soskate.cors.max-age=3600

# === Spring Boot Multipart Configuration ===
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=15MB
spring.servlet.multipart.enabled=true

# === AWS S3 Configuration ===
aws.s3.bucket-name=soskate-photos-prod
aws.s3.region=eu-west-3

# AWS Credentials (via environment variables)
aws.access-key-id=${AWS_ACCESS_KEY_ID}
aws.secret-access-key=${AWS_SECRET_ACCESS_KEY}

# === Photo Upload Configuration ===
photo.upload.max-file-size=10485760
photo.upload.allowed-formats=image/jpeg,image/png,image/heic,image/webp,image/jfif
photo.upload.min-width=400
photo.upload.min-height=400
photo.upload.max-ratio=3.0

# === Image Processing ===
photo.webp.quality-full=85
photo.webp.quality-thumb=80
photo.full.max-width=1920
photo.thumb.width=400
photo.avatar.size=800
photo.avatar-thumb.size=200

# === Photo Limits per Entity ===
photo.limits.spot-gallery=20
photo.limits.instructor-tricks=50
photo.limits.event-gallery=10

# Flyway Configuration
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true
spring.flyway.baseline-version=0
spring.flyway.validate-on-migrate=true
